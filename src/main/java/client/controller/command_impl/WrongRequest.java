package client.controller.command_impl;

import client.controller.Command;

public class WrongRequest implements Command {
    @Override
    public String execute(String request) {
        return null;
    }
}
